apiVersion: v1
kind: Service
metadata:
  name: {{ include "gitmirrorcache.fullname" . }}
  labels:
    {{- include "gitmirrorcache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gitmirrorcache.selectorLabels" . | nindent 4 }}
